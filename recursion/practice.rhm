#lang shplait

// Practice using Y combinator

// Memoization / dynamic programming

// Mutual recursion (letrec+) multiple mutually recursive definitions

// Encoding lists (recursive types)

// Recursive data 